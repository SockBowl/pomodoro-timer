import { styled } from '@mui/system';
import Container from '@mui/material/Container';

const CustomizedRowContainer = styled(Container)`
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: row;
`;

export default CustomizedRowContainer;
